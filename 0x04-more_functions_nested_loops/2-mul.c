#include <stdio.h>

/**
 * mul - function to multiply two values
 *
 * @a: first value passed in function for multiplication
 * @b: second value passed in function for multiplication
 * Return: return the a * b
 */

int mul(int a, int b)
{
	return (a * b);
}
